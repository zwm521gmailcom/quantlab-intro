# 仓库说明

本仓库是 **公开的方案介绍**（当前产品版本 `0.4.0`）。里面只有介绍、界面截图和微信二维码，没有 QuantLab 源码、数据、Token 和回测产物。

源码仓库 `quantlab` 保持 **Private**。

## 不要做的事

- 不要把源码仓库 `quantlab` 改成 Public
- 不要在本仓库提交源码、`.env`、Tushare Token、行情 Parquet、回测结果
- 不要用 git 同步 `quantlab_runtime/`
